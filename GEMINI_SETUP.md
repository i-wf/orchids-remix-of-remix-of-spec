# 🚀 دليل إعداد Google Gemini API

## ✅ تم التبديل بنجاح من OpenAI إلى Google Gemini!

تم تحديث المنصة لاستخدام **Google Gemini AI** بدلاً من OpenAI - وهو أفضل وأرخص ومجاني!

---

## 📋 لماذا Gemini؟

### **المميزات:**
- ✅ **مجاني تماماً** حتى 60 طلب/دقيقة (15 طلب/دقيقة للنماذج المتقدمة)
- ✅ **لا يحتاج بطاقة ائتمان** للبدء
- ✅ **دعم عربي ممتاز** - أفضل من GPT في اللغة العربية
- ✅ **سرعة عالية** - أسرع من GPT-4
- ✅ **سياق كبير** - يدعم حتى 2 مليون token
- ✅ **JSON Mode** - لإنشاء البطاقات التعليمية بسهولة

### **المقارنة مع OpenAI:**

| الميزة | Google Gemini | OpenAI GPT |
|--------|---------------|------------|
| **السعر** | مجاني (حتى 1500 طلب/يوم) | $0.50-$10 لكل مليون token |
| **البدء** | فوري بدون بطاقة | يحتاج بطاقة ائتمان + رصيد |
| **اللغة العربية** | ممتاز جداً | جيد |
| **السرعة** | سريع جداً | بطيء أحياناً |
| **الحدود اليومية** | 1500 طلب/يوم مجاني | حسب الرصيد |

---

## 🔑 الخطوات: كيفية الحصول على Gemini API Key

### **الخطوة 1: تسجيل الدخول**

1. اذهب إلى: **https://aistudio.google.com/app/apikey**
2. سجل دخول بحساب Google الخاص بك
3. إذا لم يكن لديك حساب Google، أنشئ واحد مجاناً

---

### **الخطوة 2: إنشاء API Key**

**الطريقة الأولى (الموصى بها):**

1. في صفحة API keys، اضغط **"Create API key"**
2. اختر:
   - **"Create API key in new project"** (إذا لم يكن لديك مشروع)
   - **أو اختر مشروع موجود** (إذا كان لديك مشروع بالفعل)
3. انتظر بضع ثوان...
4. ✅ **سيظهر المفتاح!** انسخه فوراً

**شكل المفتاح الصحيح:**
```
AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
- يبدأ بـ `AIza`
- طوله حوالي 39 حرف

---

### **الخطوة 3: إضافة المفتاح في .env**

1. افتح ملف `.env` في مجلد المشروع
2. أضف المفتاح:
   ```env
   GEMINI_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
3. احفظ الملف
4. أعد تشغيل السيرفر (إذا كان يعمل)

---

### **الخطوة 4: اختبار الـ API**

**اختبار Chatbot:**
1. افتح التطبيق
2. اذهب إلى أي صفحة
3. اضغط على أيقونة **الدردشة** (الزر العائم)
4. اكتب رسالة: "مرحباً! شرح لي قانون نيوتن الأول"
5. ✅ إذا حصلت على رد ذكي بالعربية → يعمل!

**اختبار AI Processing:**
1. اذهب إلى Dashboard المدرس
2. اختر درس به فيديو
3. افتح الدرس
4. اضغط **"إنشاء ملخص بالذكاء الاصطناعي"**
5. انتظر...
6. ✅ إذا ظهر ملخص → يعمل!

---

## 📊 الحدود المجانية

### **Gemini 1.5 Flash (المستخدم في المشروع):**

| المقياس | الحد المجاني |
|---------|--------------|
| **Requests per minute (RPM)** | 15 طلب/دقيقة |
| **Requests per day (RPD)** | 1,500 طلب/يوم |
| **Tokens per minute (TPM)** | 1 مليون token/دقيقة |

### **ماذا يعني هذا عملياً؟**

**مثال 1: Chatbot**
- كل رسالة = 1 طلب
- يمكنك إرسال **1,500 رسالة يومياً** مجاناً!
- لو عندك 100 طالب، كل واحد يرسل 15 رسالة = **مجاني تماماً!**

**مثال 2: AI Processing**
- كل درس يحتاج 3 طلبات (Transcript + Summary + Flashcards)
- يمكنك معالجة **500 درس يومياً** مجاناً!

**مثال 3: منصة متوسطة الحجم:**
- 50 طالب × 10 رسائل/يوم = 500 رسالة chatbot
- 10 دروس جديدة × 3 = 30 طلب AI processing
- **الإجمالي: 530 طلب/يوم** → **مجاني 100%!** ✅

---

## 🔧 ما تم تعديله في المشروع

### **1. Chatbot API** (`src/app/api/chatbot/route.ts`)
**قبل:**
```typescript
import OpenAI from 'openai';
const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
```

**بعد:**
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai';
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || '');
```

**الميزات:**
- ✅ ردود ذكية بالعربية من Gemini 1.5 Flash
- ✅ متخصص في المنهج المصري
- ✅ سريع ومجاني

---

### **2. AI Processing API** (`src/app/api/ai-process/route.ts`)

**الوظائف:**
- ✅ **Transcribe Video** - نسخ الفيديو لنص عربي
- ✅ **Generate Summary** - ملخص ذكي منظم
- ✅ **Generate Flashcards** - بطاقات تعليمية بصيغة JSON
- ✅ **Process All** - معالجة شاملة (كل شيء معاً)

**التحسينات:**
- استخدام `gemini-1.5-flash` للسرعة
- JSON Mode للبطاقات التعليمية
- معالجة أخطاء محسّنة

---

## 🚨 استكشاف الأخطاء

### **مشكلة 1: "API key not valid"**

**الحل:**
```bash
# تأكد من صيغة المفتاح في .env
GEMINI_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# تأكد من عدم وجود مسافات قبل/بعد المفتاح
# ❌ خطأ: GEMINI_API_KEY= AIzaSyB...
# ✅ صح: GEMINI_API_KEY=AIzaSyB...
```

---

### **مشكلة 2: "Resource has been exhausted (e.g. check quota)"**

**السبب:** تجاوزت الحد اليومي (1,500 طلب/يوم)

**الحل:**
1. انتظر حتى الغد (يتجدد كل 24 ساعة)
2. أو قلل عدد الطلبات
3. أو انشئ API key جديد بحساب Google آخر

---

### **مشكلة 3: "Chatbot لا يرد"**

**الحلول:**
1. تأكد من وجود `GEMINI_API_KEY` في `.env`
2. أعد تشغيل السيرفر:
   ```bash
   # أغلق السيرفر (Ctrl+C)
   # ثم شغله من جديد
   bun run dev
   ```
3. افتح Console وابحث عن أخطاء
4. تأكد من صحة المفتاح: https://aistudio.google.com/app/apikey

---

### **مشكلة 4: "فشل في إنشاء الملخص"**

**الحلول:**
1. تأكد من وجود فيديو في الدرس (videoUrl)
2. جرب `action: "process-all"` بدلاً من `summarize` فقط
3. تأكد من الإنترنت وعمل Gemini API

---

## 🎯 الاستخدام المتقدم

### **استدعاء API Processing يدوياً:**

```javascript
// إنشاء كل شيء دفعة واحدة (الأفضل!)
const response = await fetch('/api/ai-process', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    lessonId: 123,
    action: 'process-all' // ← Transcript + Summary + Flashcards
  })
});

// أو اختر action محدد:
// - 'transcribe' → نسخ الفيديو فقط
// - 'summarize' → ملخص فقط
// - 'generate-flashcards' → بطاقات فقط
// - 'process-all' → كل شيء معاً
```

---

## 📦 المكتبات المستخدمة

```json
{
  "@google/generative-ai": "^0.24.1"
}
```

تم تثبيتها تلقائياً! ✅

---

## 📞 الدعم

**إذا واجهت أي مشكلة:**

1. **تحقق من API Key:**
   - https://aistudio.google.com/app/apikey
   - تأكد من نسخه بشكل صحيح

2. **راجع Console Logs:**
   - افتح Console في المتصفح (F12)
   - ابحث عن أخطاء حمراء

3. **تأكد من .env:**
   ```env
   GEMINI_API_KEY=AIzaSyB...
   ```

4. **أعد تشغيل السيرفر:**
   ```bash
   # Ctrl+C لإيقاف السيرفر
   bun run dev  # لتشغيله مرة أخرى
   ```

---

## 🎉 ملخص سريع

### **✅ ما تم إنجازه:**
1. ✅ إزالة OpenAI والتبديل لـ Google Gemini
2. ✅ تحديث Chatbot API
3. ✅ تحديث AI Processing API (Transcript, Summary, Flashcards)
4. ✅ تحديث ملف `.env` مع `GEMINI_API_KEY`

### **🚀 الخطوة التالية:**
1. احصل على Gemini API Key من: **https://aistudio.google.com/app/apikey**
2. أضفه في `.env`: `GEMINI_API_KEY=AIza...`
3. جرب الـ Chatbot!
4. جرب إنشاء ملخص AI!

---

## 💡 نصائح مهمة

**✅ افعل:**
- استخدم Gemini 1.5 Flash للسرعة والمجانية
- استخدم `process-all` لتوفير الطلبات
- راقب الحدود اليومية (1,500 طلب/يوم)

**❌ لا تفعل:**
- لا تشارك API Key مع أحد
- لا تستخدم أكثر من 1,500 طلب/يوم (أو ستتجاوز الحد المجاني)
- لا تنسى إعادة تشغيل السيرفر بعد تغيير `.env`

---

**🎊 منصتك التعليمية الآن تعمل بـ Google Gemini - مجاناً وبسرعة فائقة!** 🚀
